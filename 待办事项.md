# 待办事项

## GatewayProcessor#processRequest 全流程

### 流程图

```
processRequest(RequestContext)
│
├─ 【1. validateContext】验证上下文
│   └─ 检查 RequestContext、ServerExchange、Request 非空
│
├─ 【2. prepareRequest】同步准备阶段 (直接调用，无线程切换)
│   │
│   ├─ 【2.1 路由匹配】routeManager.matchRoute(context)
│   │   └─ 设置 context.setMatchedRoute(route)
│   │
│   ├─ 【2.2 前置过滤器】executeFilters(context, FilterType.PRE)
│   │   └─ 遍历路由的 PRE 类型过滤器并执行
│   │
│   ├─ 【2.3 端点选择】route.getService().selectTarget(context, strategy)
│   │   └─ 设置 context.setSelectedEndpoint(endpoint)
│   │
│   └─ 【2.4 连接获取】connectionPoolManager.getClientConnection(endpoint, protocol)
│       └─ 设置 context.setClientConnection(connection)
│
├─ 【3. invokeBackendService】异步调用阶段 (唯一的线程切换点)
│   │
│   ├─ clientConnection.send(request)
│   │   └─ 返回 CompletableFuture<Message>
│   │
│   └─ thenAccept(response → 在 I/O 线程中执行)
│       ├─ 【3.1】setResponseToExchange(context, response)
│       ├─ 【3.2】executeFilters(context, FilterType.POST)
│       └─ 【3.3】sendResponse(context)
│
└─ 【4. handleCompletion】完成处理
    ├─ 如果有错误 → handleError(context, error)
    ├─ cleanupResources(context)
    └─ context.markComplete()
```

---

## 待实现功能清单

| 优先级 | 序号 | 功能 | 状态 | 位置 | 说明 |
|--------|------|------|------|------|------|
| P1 | 1 | RouteManager.matchRoute() | 🟢 已完成 | DefaultRouteManager:78-159 | 路由匹配核心逻辑 |
| P1 | 2 | RouteService.selectTarget() | 🟢 已完成 | ConfigRouteService/DiscoveryRouteService | 负载均衡端点选择 |
| P1 | 3 | setResponseToExchange() | 🟢 已完成 | GatewayProcessor:196-251 | 后端响应设置到 Exchange |
| P2 | 4 | ConnectionPoolManager.getClientConnection() | 🔴 未实现 | ConnectionPoolManager | 获取后端连接 |
| P2 | 5 | ClientConnection.send() | 🔴 未实现 | ClientConnection 接口 | 发送请求到后端 |
| P2 | 6 | ServerConnection.sendResponse() | 🔴 未实现 | ServerConnection 接口 | 发送响应给客户端 |
| P2 | 7 | ServerConnection.sendError() | 🔴 未实现 | ServerConnection 接口 | 发送错误响应 |
| P3 | 8 | PRE/POST 过滤器实现 | 🔴 未实现 | Filter 接口 | 各种业务过滤器 |
| P3 | 9 | 负载均衡策略实现 | 🔴 未实现 | LoadBalanceStrategy | RoundRobin 等策略 |
| P3 | 10 | 协议转换器 | 🔴 未实现 | HTTP→gRPC 等 | 跨协议调用 |

---

## 优先级说明

### P1 - 核心流程（必须先实现）
1. **RouteManager.matchRoute()** - 路由匹配，整个流程的入口
2. **RouteService.selectTarget()** - 端点选择，配合负载均衡
3. **setResponseToExchange()** - 响应设置，完成请求-响应闭环

### P2 - 连接层
4. **ConnectionPoolManager.getClientConnection()** - 连接池管理
5. **ClientConnection.send()** - 后端服务调用
6. **ServerConnection.sendResponse()** - 响应发送给客户端

### P3 - 增强功能
7. **PRE/POST 过滤器** - 请求/响应处理（认证、日志、限流等）
8. **负载均衡策略** - 高可用保障
9. **协议转换** - 跨协议支持（HTTP→gRPC）

---

## 进度追踪

### 已完成 ✅
- [x] gateway-routes.yml 配置文件解析（GatewayConfigLoader）
- [x] GatewayRouteConfig 配置模型定义
- [x] RouteConfigConverter 路由配置转换器
- [x] GatewayBootstrap 配置加载和路由注册
- [x] RouteManager.matchRoute() 路由匹配逻辑（性能优化实现）
- [x] RouteService.selectTarget() 端点选择逻辑（ConfigRouteService/DiscoveryRouteService 优化）
- [x] setResponseToExchange() 响应设置逻辑（支持协议兼容性检查和状态日志）

### 进行中 🔄
- [ ] 待定

### 待办 📋
- [ ] 实现 ConnectionPoolManager 连接池管理
- [ ] 实现 ClientConnection.send() 后端调用
- [ ] 实现 ServerConnection.sendResponse() 响应发送
- [ ] 实现 ServerConnection.sendError() 错误响应
- [ ] 实现各种 PRE/POST 过滤器
- [ ] 实现负载均衡策略（RoundRobin、WeightedRoundRobin、LeastConnections 等）
- [ ] 实现协议转换器

---

## 变更记录

| 日期 | 内容 | 操作人 |
|------|------|--------|
| 2025-01-23 | 创建文档，梳理 GatewayProcessor#processRequest 全流程 | Claude |
| 2025-01-23 | 实现 RouteManager.matchRoute() 路由匹配逻辑 | Claude |
| 2025-01-23 | 优化 RouteService.selectTarget() 端点选择逻辑 | Claude |
| 2025-01-23 | 实现 setResponseToExchange() 响应设置逻辑 | Claude |
