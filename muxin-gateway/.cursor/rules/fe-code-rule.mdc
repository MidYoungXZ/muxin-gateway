---
description: 
globs: 
alwaysApply: false
---
---
description: 专门适用于前端开发的编码规则和约束，基于Vue3+TypeScript+ElementPlus技术栈
globs: 
  - "gateway-admin-ui/**"
  - "**/*.vue"
  - "**/*.ts" 
  - "**/*.js"
  - "**/*.scss"
  - "**/*.css"
alwaysApply: false
---
### 通用规则
1. 默认情况下，所有回复都必须是中文
2. 复杂需求拆解成小任务，分步实现，每完成一个小任务后再继续  
3. 代码实现前后要仔细检查，确保没有遗漏  
4. 在已有功能基础上添加新功能时，必须确保：  
   - 不影响原有功能  
   - 不添加其他功能、代码、逻辑、文件、配置、依赖  
5. 遵循 Vue 3 + TypeScript + Element Plus 架构设计，保持代码风格一致  
6. 代码修改遵循单一职责原则，不混合多个变更  
7. 在进行代码设计规划的时候，请符合"第一性原理"
8. 在代码实现的时候，请符合"KISS原则"和"SOLID原则"
9. 尽量复用已有组件、composables、工具函数，避免重复代码  
10. 不引入不必要的第三方依赖，避免增加打包体积和维护成本  
11. 确保代码可读性与可维护性，必要时加简要注释  
12. 代码变更范围最小化，避免大范围修改
13. 实现后进行基本逻辑自检和UI自检，确保无错误  
14. 如果有疑问，先询问再修改，不要擅自做决定  

### 前端技术栈约束
15. 严格使用 TypeScript，所有新增文件必须是 .ts 或 .vue 格式，禁用 any 类型
16. 组件开发必须使用 Vue 3 Composition API + `<script setup>` 语法，禁用 Options API
17. 状态管理统一使用 Pinia，按模块组织 stores，禁止在组件间直接传递复杂状态
18. UI 组件库优先使用 Element Plus，自定义组件需符合 Element Plus 设计规范
19. 样式开发使用 SCSS，所有CSS变量统一定义在 `@/styles/variables.scss`
20. 网络请求统一使用项目封装的 axios 实例（`@/utils/request.ts`），不直接使用 axios
21. 路由配置必须使用懒加载方式（defineAsyncComponent），避免首屏加载过大
22. 图标统一使用 `@element-plus/icons-vue`，避免引入其他图标库
23. 日期处理统一使用 dayjs，工具函数使用 lodash-es
24. 图表组件统一使用 echarts

### 代码组织与命名规范  
25. 组件文件命名使用 PascalCase，如 `UserFormDialog.vue`
26. 页面组件放在 `src/views/` 下，按功能模块分目录，公共组件放在 `src/components/`
27. Composables 放在 `src/composables/` 下，文件名以 `use` 开头
28. 类型定义统一放在 `src/types/` 下，文件名以 `.d.ts` 结尾
29. API 接口定义放在 `src/api/` 下，按业务模块划分文件
30. 工具函数放在 `src/utils/` 下，按功能分类
31. 样式文件放在 `src/styles/` 下，组件私有样式使用 `<style scoped>`
32. 常量定义放在单独的 constants 文件中，使用 UPPER_SNAKE_CASE 命名

### Vue组件开发规范
33. 组件必须定义 `name` 属性（通过文件名自动推导或显式定义）
34. Props 必须定义完整的 TypeScript 类型，提供默认值和验证
35. 使用 `defineEmits<T>()` 定义组件事件类型，避免直接修改 props
36. 大组件必须拆分为小组件，单个 .vue 文件不超过 300 行
37. 使用 provide/inject 进行深层组件通信，避免 props 层层传递
38. 合理使用 teleport 处理弹窗、遮罩等脱离文档流的元素
39. 表单组件必须支持 v-model，实现双向绑定
40. 列表组件必须正确设置 key 属性，优先使用唯一ID

### TypeScript类型规范
41. 启用严格模式，禁止使用 any 类型，必要时使用 unknown 或类型断言
42. 接口定义必须导出并添加 JSDoc 注释说明
43. 组件 Props 和 Emits 必须定义明确的 TypeScript 接口
44. 优先使用 interface 定义对象类型，type 用于联合类型和工具类型
45. API 响应数据必须定义对应的 TypeScript 接口
46. 使用泛型提高代码复用性，避免类型重复定义
47. 合理使用工具类型（Partial、Pick、Omit等）简化类型定义

### 样式开发规范
48. 优先使用 CSS 变量（定义在 variables.scss），保持主题一致性
49. 响应式设计使用项目定义的 SCSS mixins，避免硬编码断点
50. 类名使用 BEM 命名规范或语义化命名，避免样式冲突
51. 动画使用 CSS transition 和 transform，避免引入重型动画库
52. 避免使用深度选择器（:deep()），优先调整组件结构
53. 颜色值统一使用 CSS 变量，不写死色值
54. 间距使用预定义的 space 变量（--space-*）

### 性能优化规范
55. 大列表或表格必须使用虚拟滚动或分页加载
56. 图片资源必须进行懒加载处理，使用 Element Plus 的 Image 组件
57. 避免在模板中使用复杂计算，使用 computed 替代
58. 合理使用 v-memo 和 v-once 指令优化渲染性能
59. 使用 defineAsyncComponent 进行大组件懒加载
60. 避免在循环中创建新的对象或函数，提取到组件外部
61. 大数据集合使用 shallowRef 或 markRaw 避免深度响应式
62. 路由切换时正确清理定时器、事件监听器等副作用

### 自动化执行与安全策略
63. 自动执行无需严格确认的操作，减少人为干预，提高执行效率：  
   - 自动执行 lint、格式化、类型检查等验证流程  
   - 删除、移动、重命名文件等常规操作无需额外确认  
   - npm 脚本执行（dev、build、preview）可直接执行  
   - 涉及影响较大的操作（修改构建配置、删除重要文件）仍需确认  
64. 重要操作（如删除页面文件、修改路由配置）应提醒影响范围
65. 修改 package.json 依赖时，需要说明变更原因和版本兼容性
66. 生产构建前自动执行 TypeScript 类型检查和 ESLint 检查

### 代码质量优化
67. 代码生成后，自动进行 ESLint 和 Prettier 格式化
68. 自动移除未使用的 import、变量和类型定义
69. 对于可能影响性能的代码（频繁渲染、深层响应式对象），提供优化建议  
70. 关键功能必须提供异常处理和用户友好的错误提示
71. 表单验证规则要完整，错误提示要清晰友好
72. 网络请求必须有 loading 状态和完善的错误处理机制
73. 异步操作要有适当的防抖或节流处理

### 状态管理规范
74. Pinia store 按业务模块划分，一个模块一个 store 文件
75. store 中的 state 必须定义初始值和 TypeScript 类型
76. actions 中的异步操作必须有错误处理
77. 避免在 store 中直接操作 DOM 或调用浏览器 API
78. 复杂的计算逻辑使用 getters，简单的使用 computed

### 国际化与可访问性
79. 所有用户可见文本必须使用 i18n 国际化，禁止硬编码中文
80. 重要操作按钮必须有 loading 状态和禁用状态
81. 表单元素必须有正确的 label 和 placeholder
82. 关键功能要支持键盘操作，遵循无障碍访问标准

### 架构感知
83. AI 代码编辑器应优先分析现有前端代码结构，避免重复实现已有功能  
84. 在添加新功能时，优先扩展现有页面和组件，而非从零编写  
85. 如遇前端架构不清晰的情况，先整理组件依赖关系，再执行修改
86. 新增页面必须考虑权限控制、路由配置和面包屑导航
87. 确保新功能与现有 UI 设计风格和交互模式保持一致

88. 复用现有的 API 接口和数据结构，避免重复定义