# 编码原则与规范

## 健壮性原则

- **防御性编程**：验证所有外部输入（参数、API响应、用户输入）
- **错误处理**：使用try-catch，避免吞掉异常，提供有意义的错误信息
- **空值检查**：使用可选链、空值合并运算符或显式null检查
- **边界条件**：处理空数组、空字符串、零值、极大/极小值
- **类型安全**：使用TypeScript、类型注解，避免any类型
- **资源管理**：及时释放资源（文件句柄、数据库连接），使用finally块
- **日志记录**：关键操作、错误、异常路径都要记录日志

## 扩展性原则

- **单一职责**：每个函数/类只做一件事
- **开闭原则**：对扩展开放，对修改关闭（使用策略模式、工厂模式）
- **依赖注入**：依赖抽象接口而非具体实现
- **配置外部化**：硬编码值移到配置文件
- **接口隔离**：接口小而专注，不强迫实现不需要的方法
- **组合优于继承**：使用组合实现代码复用

## 代码质量

- **命名清晰**：变量、函数、类名要自解释
- **函数短小**：单一函数不超过20-50行
- **DRY原则**：避免重复代码，提取公共逻辑
- **注释适度**：复杂逻辑添加注释，简单代码无需注释
- **测试覆盖**：关键路径有单元测试，边界条件有测试

## 编码后自检原则（简化版）

### 编译错误检查
- 语法错误
- 类型错误
- 依赖缺失
- 构建错误

### 逻辑错误检查
- 函数执行结果是否符合预期
- 边界条件处理是否正确
- 数据流是否正确
- 条件判断逻辑是否准确
