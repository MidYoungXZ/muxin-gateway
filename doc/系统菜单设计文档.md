# Muxin Gateway 系统菜单设计文档

> 基于《网关系统概要设计文档.md》第1-29行内容生成
> 文档版本：v1.0
> 创建日期：2025-01-09

---

## 一、菜单体系总览

### 1.1 菜单分类结构

```
Muxin Gateway 管理系统
├── 🏠 首页（仪表板）
├── 📈 监控大屏
├── 📌 路由管理
├── 🔒 鉴权规则
└── 🧩 系统管理
```

### 1.2 菜单类型说明

| 类型 | 代码标识 | 说明 | 示例 |
|------|---------|------|------|
| 目录 | M | 一级菜单，包含子菜单 | 监控大屏、路由管理 |
| 菜单 | C | 具体功能页面 | 路由列表、用户管理 |
| 按钮 | F | 页面内操作按钮权限 | 新增、编辑、删除 |

---

## 二、详细菜单设计

### 2.1 首页（仪表板）

| 菜单代码 | 菜单名称 | 类型 | 路由路径 | 图标 | 完成状态 | 说明 |
|---------|---------|------|---------|------|---------|------|
| dashboard | 首页 | C | /dashboard | House | ✅ 已完成 | 系统概览仪表板 |

**功能说明**：
- 展示网关运行状态概览
- 快捷入口卡片

---

### 2.2 监控大屏 📈

| 菜单代码 | 菜单名称 | 类型 | 路由路径 | 排序 | 完成状态 | 功能说明 |
|---------|---------|------|---------|------|---------|---------|
| monitor | 监控大屏 | M | /monitor | 1 | 🚧 部分完成 | 监控功能目录 |
| ├─ monitor:realtime | 实时请求统计 | C | /monitor/realtime | 1 | 🚧 部分完成 | 展示实时QPS、响应时间、错误率、状态码分布 |
| ├─ monitor:route | 路由调用监控 | C | /monitor/routes | 2 | ❌ 未实现 | 路由调用趋势、异常分布、慢接口排行 |
| └─ monitor:filter | 过滤器命中监控 | C | /monitor/filters | 3 | ❌ 未实现 | 过滤器命中次数与影响范围统计 |

**完成情况分析**：

| 子菜单 | 框架实现 | 数据接入 | 图表渲染 | 实时推送 | 整体进度 |
|--------|---------|---------|---------|---------|---------|
| 实时请求统计 | ✅ | ❌ | ✅ | ❌ | 60% |
| 路由调用监控 | ✅ | ❌ | ❌ | ❌ | 20% |
| 过滤器命中监控 | ✅ | ❌ | ❌ | ❌ | 20% |

**待开发功能**：
- [ ] WebSocket 实时数据推送服务
- [ ] 监控数据采集与聚合
- [ ] 路由调用趋势图表
- [ ] 过滤器命中率统计

---

### 2.3 路由管理 📌

| 菜单代码 | 菜单名称 | 类型 | 路由路径 | 排序 | 完成状态 | 功能说明 |
|---------|---------|------|---------|------|---------|---------|
| routes | 路由管理 | M | /routes | 2 | ✅ 基本完成 | 路由配置管理目录 |
| ├─ routes:list | 路由列表 | C | /routes/list | 1 | ✅ 已完成 | 管理所有动态路由配置，支持CRUD、启用禁用、搜索 |
| ├─ routes:test | 路由测试工具 | C | /routes/test | 2 | ❌ 未实现 | 模拟请求工具，验证路由转发 |
| ├─ routes:nodes | 服务管理 | C | /routes/nodes | 3 | 🚧 部分完成 | 后端服务节点管理，上下线、健康探测 |
| ├─ routes:loadbalance | 负载均衡配置 | C | /routes/loadbalance | 4 | 🚧 部分完成 | 负载均衡策略配置（仅轮询实现） |
| └─ routes:filters | 过滤器管理 | C | /routes/filters | 5 | 🚧 部分完成 | 过滤器配置（基础过滤器实现） |

**完成情况分析**：

| 子菜单 | 页面框架 | API接口 | 数据交互 | 功能完整度 | 整体进度 |
|--------|---------|---------|---------|-----------|---------|
| 路由列表 | ✅ | ✅ | ✅ | 100% | 100% |
| 路由测试工具 | ✅ | ❌ | ❌ | 0% | 20% |
| 服务管理 | ✅ | 🚧 | 🚧 | 60% | 60% |
| 负载均衡配置 | ✅ | 🚧 | 🚧 | 40% | 50% |
| 过滤器管理 | ✅ | 🚧 | 🚧 | 50% | 60% |

**已实现功能**：
- ✅ 路由列表 CRUD
- ✅ 路由启用/禁用
- ✅ 路由搜索
- ✅ 断言配置（PATH、METHOD、HEADER）
- ✅ 基础过滤器配置

**待开发功能**：
- [ ] 路由测试工具（模拟请求、调试）
- [ ] 服务节点健康检查
- [ ] 加权轮询、最少连接、IP哈希等负载均衡策略
- [ ] 限流、熔断、重试等高级过滤器

---

### 2.4 鉴权规则 🔒

| 菜单代码 | 菜单名称 | 类型 | 路由路径 | 排序 | 完成状态 | 功能说明 |
|---------|---------|------|---------|------|---------|---------|
| auth | 鉴权规则 | M | /auth | 3 | ❌ 未实现 | 鉴权规则配置目录 |
| ├─ auth:api | API权限控制 | C | /auth/api-permission | 1 | ❌ 未实现 | 配置路由访问权限（用户/角色/IP等限制） |
| └─ auth:whitelist | 白名单配置 | C | /auth/whitelist | 2 | ❌ 未实现 | 配置免鉴权IP/Token/Header白名单 |

**完成情况分析**：

| 子菜单 | 页面框架 | 后端API | 数据表 | 业务逻辑 | 整体进度 |
|--------|---------|---------|--------|---------|---------|
| API权限控制 | ✅ | ❌ | ❌ | ❌ | 10% |
| 白名单配置 | ✅ | ❌ | ❌ | ❌ | 10% |

**待开发功能**：
- [ ] API 权限规则数据表设计
- [ ] 权限规则配置页面
- [ ] 白名单规则管理
- [ ] 权限规则拦截器
- [ ] IP/Token/Header 验证逻辑

---

### 2.5 系统管理 🧩

| 菜单代码 | 菜单名称 | 类型 | 路由路径 | 排序 | 完成状态 | 功能说明 |
|---------|---------|------|---------|------|---------|---------|
| system | 系统管理 | M | /system | 4 | ✅ 基本完成 | 系统配置管理目录 |
| ├─ system:user | 用户管理 | C | /system/users | 1 | ✅ 已完成 | 用户账号CRUD、角色绑定、密码重置 |
| ├─ system:role | 角色管理 | C | /system/roles | 2 | ✅ 已完成 | 角色定义、权限关联（权限验证禁用） |
| ├─ system:dept | 部门管理 | C | /system/departments | 3 | ❌ 未实现 | 树形组织结构、拖拽移动 |
| ├─ system:permission | 权限管理 | C | /system/permissions | 4 | ✅ 基础完成 | 权限点管理（菜单/接口/按钮） |
| ├─ system:log | 操作日志 | C | /system/operation-logs | 5 | 🚧 部分完成 | 操作/登录日志查看、搜索、导出 |
| └─ system:config | 系统配置 | C | /system/config | 6 | ❌ 未实现 | 系统参数配置、热刷新 |

**完成情况分析**：

| 子菜单 | 页面实现 | 后端API | 数据表 | 功能完整度 | 整体进度 |
|--------|---------|---------|--------|-----------|---------|
| 用户管理 | ✅ | ✅ | ✅ | 100% | 100% |
| 角色管理 | ✅ | ✅ | ✅ | 90% | 90% |
| 部门管理 | ✅ | ❌ | ✅ | 0% | 20% |
| 权限管理 | ✅ | ✅ | ✅ | 80% | 80% |
| 操作日志 | ✅ | 🚧 | ✅ | 60% | 70% |
| 系统配置 | ✅ | ❌ | ❌ | 0% | 10% |

**已实现功能**：
- ✅ 用户管理（完整 CRUD）
- ✅ 角色管理（完整 CRUD）
- ✅ 权限树形展示
- ✅ 基础操作日志记录

**待开发功能**：
- [ ] 部门树形管理（拖拽、移动）
- [ ] 部门数据权限隔离
- [ ] 操作日志高级查询和导出
- [ ] 系统配置项管理
- [ ] 配置热刷新功能
- [ ] 权限验证启用

---

## 三、菜单权限标识规范

### 3.1 权限标识命名规则

```
{模块}:{资源}:{操作}
```

**示例**：
- `routes:list:add` - 路由列表新增按钮
- `system:user:edit` - 用户管理编辑按钮
- `monitor:realtime:view` - 实时统计查看权限

### 3.2 通用操作权限

| 操作 | 权限后缀 | 说明 |
|------|---------|------|
| 查看 | :view | 查看页面/数据 |
| 新增 | :add | 新增数据 |
| 编辑 | :edit | 修改数据 |
| 删除 | :delete | 删除数据 |
| 导出 | :export | 导出数据 |
| 导入 | :import | 导入数据 |
| 启用 | :enable | 启用功能 |
| 禁用 | :disable | 禁用功能 |

---

## 四、菜单完成度统计

### 4.1 总体完成情况

| 一级菜单 | 子菜单总数 | 已完成 | 部分完成 | 未开始 | 完成率 |
|---------|----------|--------|---------|--------|--------|
| 首页 | 1 | 1 | 0 | 0 | 100% |
| 监控大屏 | 3 | 0 | 1 | 2 | 33% |
| 路由管理 | 5 | 1 | 3 | 1 | 70% |
| 鉴权规则 | 2 | 0 | 0 | 2 | 10% |
| 系统管理 | 6 | 2 | 2 | 2 | 60% |
| **合计** | **17** | **4** | **6** | **7** | **55%** |

### 4.2 完成度分级

| 完成状态 | 数量 | 菜单 |
|---------|------|------|
| ✅ 已完成（100%） | 4 | 首页、路由列表、用户管理、角色管理 |
| 🚧 部分完成（50-99%） | 6 | 实时请求统计、服务管理、负载均衡配置、过滤器管理、权限管理、操作日志 |
| ❌ 未实现（0-49%） | 7 | 路由调用监控、过滤器命中监控、路由测试工具、API权限控制、白名单配置、部门管理、系统配置 |

---

## 五、数据库菜单表结构

### 5.1 sys_menu 表字段说明

```sql
CREATE TABLE `sys_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '菜单ID',
  `parent_id` bigint DEFAULT '0' COMMENT '父菜单ID，0表示顶级菜单',
  `ancestors` varchar(500) DEFAULT '' COMMENT '祖级列表，用逗号分隔',
  `menu_name` varchar(50) NOT NULL COMMENT '菜单名称',
  `i18n_code` varchar(100) DEFAULT NULL COMMENT '国际化编码',
  `menu_type` char(1) NOT NULL COMMENT '菜单类型：M-目录，C-菜单，F-按钮',
  `path` varchar(200) DEFAULT '' COMMENT '路由地址',
  `component` varchar(200) DEFAULT NULL COMMENT '组件路径',
  `perms` varchar(100) DEFAULT NULL COMMENT '权限标识',
  `icon` varchar(100) DEFAULT '#' COMMENT '菜单图标',
  `sort_order` int DEFAULT '0' COMMENT '排序',
  `visible` tinyint DEFAULT '1' COMMENT '是否显示：0-隐藏，1-显示',
  `status` tinyint DEFAULT '1' COMMENT '状态：0-禁用，1-启用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `create_by` varchar(50) DEFAULT NULL COMMENT '创建人',
  `update_by` varchar(50) DEFAULT NULL COMMENT '更新人',
  `deleted` tinyint DEFAULT '0' COMMENT '删除标记：0-正常，1-删除',
  PRIMARY KEY (`id`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单权限表';
```

---

## 六、前端路由配置

### 6.1 路由配置示例

```typescript
// src/router/index.ts
const routes: RouteRecordRaw[] = [
  {
    path: '/',
    component: Layout,
    redirect: '/dashboard',
    children: [
      // 监控大屏
      {
        path: '/monitor',
        name: 'Monitor',
        redirect: '/monitor/realtime',
        meta: { title: '监控大屏', icon: 'Monitor' },
        children: [
          {
            path: '/monitor/realtime',
            name: 'RealtimeStats',
            component: () => import('@/views/monitor/realtime/index.vue'),
            meta: { title: '实时请求统计' }
          }
          // ... 其他子路由
        ]
      },
      // ... 其他模块
    ]
  }
]
```

---

## 七、开发优先级建议

### 7.1 高优先级（P0）

| 菜单 | 待开发功能 | 预计工作量 |
|------|-----------|-----------|
| 监控大屏 | 实时数据接入、WebSocket推送 | 3天 |
| 路由管理 | 路由测试工具 | 2天 |
| 系统管理 | 权限验证启用 | 2天 |

### 7.2 中优先级（P1）

| 菜单 | 待开发功能 | 预计工作量 |
|------|-----------|-----------|
| 系统管理 | 部门管理功能 | 3天 |
| 鉴权规则 | API权限控制、白名单 | 4天 |
| 系统管理 | 系统配置管理 | 2天 |

### 7.3 低优先级（P2）

| 菜单 | 待开发功能 | 预计工作量 |
|------|-----------|-----------|
| 监控大屏 | 路由调用监控、过滤器监控 | 5天 |
| 路由管理 | 高级负载均衡策略 | 3天 |
| 系统管理 | 操作日志高级查询 | 2天 |

---

## 八、附录

### 8.1 国际化配置

```typescript
// src/i18n/locales/zh-CN.ts
export default {
  menu: {
    monitor: '监控大屏',
    realtimeStats: '实时请求统计',
    routeMonitor: '路由调用监控',
    filterMonitor: '过滤器命中监控',
    routes: '路由管理',
    routeList: '路由列表',
    // ... 其他菜单项
  }
}
```

### 8.2 菜单API接口

```typescript
// src/api/menus.ts
export const menuApi = {
  getUserMenuTree: () => request.get('/api/menus/user-tree'),
  getMenuTree: () => request.get('/api/menus/tree'),
  create: (data) => request.post('/api/menus', data),
  update: (id, data) => request.put(`/api/menus/${id}`, data),
  delete: (id) => request.delete(`/api/menus/${id}`)
}
```

### 8.3 菜单实体类

```java
/**
 * 菜单实体
 */
@Data
@Table("sys_menu")
public class SysMenu {

    @Id(keyType = KeyType.Auto)
    private Long id;

    /** 父菜单ID */
    private Long parentId;

    /** 祖级列表 */
    private String ancestors;

    /** 菜单名称 */
    private String menuName;

    /** 国际化编码 */
    private String i18nCode;

    /** 菜单类型：M-目录，C-菜单，F-按钮 */
    private String menuType;

    /** 路由地址 */
    private String path;

    /** 组件路径 */
    private String component;

    /** 权限标识 */
    private String perms;

    /** 菜单图标 */
    private String icon;

    /** 排序 */
    @Column("sort_order")
    private Integer sortOrder;

    /** 是否显示：0-隐藏，1-显示 */
    private Integer visible;

    /** 状态：0-禁用，1-启用 */
    private Integer status;

    /** 创建时间 */
    private LocalDateTime createTime;

    /** 更新时间 */
    private LocalDateTime updateTime;

    /** 创建人 */
    private String createBy;

    /** 更新人 */
    private String updateBy;

    /** 是否删除：0-否，1-是 */
    @Column(isLogicDelete = true)
    private Integer deleted;
}
```

---

**文档结束**
